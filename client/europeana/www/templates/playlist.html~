<ion-view view-title="Playlist">
  <ion-content class="padding location">
    {{playlistId}}
    <h2>{{playlist.title}}</h2>
    <img src="{{playlist.thumbnail}}">
    <p><i class="ion-pricetags"></i> tags</p>
  </ion-content>
</ion-view>
